name: Workstation
root: ~/.workstation

tmux_options: -2

attach: false

windows:
  - vim: vim
  - shell:
  - shell:
  - shell:
  - shell:
  - shell:
  - claude: INTER_SESSION_NAME=workstation claude --continue
