name: ReBAC Schema Registry
root: ~/workspace/rebac-schema-registry

tmux_options: -2

attach: false

windows:
  - vim: vim
  - server:
  - db:
  - console:
  - shell:
  - shell2:
  - claude: claude --continue
