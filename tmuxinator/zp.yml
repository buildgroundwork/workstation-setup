name: ZP
root: ~/workspace/zenpayroll

tmux_options: -2

attach: false

windows:
  - vim: vim
  - server:
      layout: main-vertical
      panes:
        - # server started by claude
        - claude "start the server"
  - db: bundle exec rails db
  - console: bundle exec rails c
  - shell:
      layout: main-vertical
      panes:
        -
        - claude
  # - sidekiq: bundle exec sidekiq
  # - webpack: bin/webpack-dev-server

