# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-resurrect'
# Rebind resurrect save from Ctrl-s to Alt-s. The default Ctrl-b Ctrl-s is too
# easy to trigger accidentally when quickly typing Ctrl-b s (session chooser),
# and saving sessions is slow and non-interruptible.
set -g @resurrect-save 'M-s'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-pain-control'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

set -g status-left "#{prefix_highlight}"
set -g status-right "#(/Users/adam.milligan/.claude/plugins/cache/gusto-claude-code/claude-tmux-attention/0.1.1/tmux/status.sh) #{session_name} |#{cpu_bg_color} CPU: #{cpu_icon} #{cpu_percentage} #[bg=default]| %a %Y-%m-%d %H:%M"
set -g status-right-length 150

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'

# Prevent automatic resizing of tmux windows
set -g window-size smallest

# Send Shift-Enter properly
bind-key -n S-Enter send-keys Escape "[13;2u"
set -s extended-keys on
set -as terminal-features 'xterm-256color:extkeys'

# Make Escape (and Ctrl-[) exit copy-mode instead of just clearing selection
bind-key -T copy-mode-vi Escape send-keys -X cancel

# Claude session chord: prefix + A enters the "claude" key-table, then:
#   A -> attention popup (pending Claude sessions)
#   D -> hub dashboard (toggle the pinned status pane)
# So the full chords are: Ctrl-b A A  and  Ctrl-b A D.
bind-key A switch-client -T claude
bind-key -T claude A run-shell "/Users/adam.milligan/.claude/plugins/cache/gusto-claude-code/claude-tmux-attention/0.1.1/tmux/popup.sh"
bind-key -T claude D run-shell "~/.claude/skills/hub/scripts/hub-status-pane.sh toggle"

# >>> claude-tmux-attention >>>
# Added by claude-tmux-attention setup. To remove, run:
#   /Users/adam.milligan/.claude/plugins/cache/gusto-claude-code/claude-tmux-attention/0.1.1/scripts/setup.sh --uninstall

# Keybinding: prefix + A enters the claude chord table; A then opens the popup.
# (Defined above with the dashboard chord so both live together.)

# Status-line indicator (optional, opt-in):
# Append the following to your status-right or status-left to show the
# pending count in your tmux status bar:
#
#   set -ag status-right " #(/Users/adam.milligan/.claude/plugins/cache/gusto-claude-code/claude-tmux-attention/0.1.1/tmux/status.sh)"
#
# When zero Claude sessions are pending, the script prints nothing and
# the indicator takes no space. When non-zero, it prints in colour203
# (bright red) by default. Override with env vars CLAUDE_TMUX_ATTENTION_FG,
# CLAUDE_TMUX_ATTENTION_BG, CLAUDE_TMUX_ATTENTION_GLYPH.
#
# Note: use 'set -ag' (not '-agF'). The -F flag interferes with #() shell
# expansion in the appended value.
# <<< claude-tmux-attention <<<
